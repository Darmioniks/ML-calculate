# ML-calculate
# Рукописный калькулятор на базе AI

Этот проект — интерактивное приложение на Python, которое распознает рукописные математические выражения и вычисляет их результат. Пользователь рисует два числа и знак операции в трех окнах, а модель Random Forest распознает символы и выдает ответ.

## 🚀 Возможности
- **3 холста для рисования**: Отдельные зоны для первого числа, знака и второго числа.
- **Распознавание (ML)**: Используется модель Random Forest из `scikit-learn`.
- **Обучение на лету**: Если робот ошибся, вы можете тут же сохранить свой вариант и дообучить модель.
- **Оценка точности**: Встроенный инструмент показывает точность в % и строит матрицу ошибок.
- **Мягкая кисть**: Реализовано сглаживание (anti-aliasing) при рисовании для повышения точности.

## 🛠️ Стек технологий
- Python 3.x
- Tkinter (GUI)
- Scikit-learn (Random Forest)
- Pandas & NumPy
- Matplotlib & Seaborn (Графики)

## 📦 Установка

1. **Клонируйте репозиторий или скачайте файлы**
2. **Установите зависимости**:
   Откройте терминал в папке с проектом и выполните:

## ▶️ Запуск

### 1. Обучение модели
Перед первым запуском (или после добавления новых данных) нужно обучить модель.
Запустите `train.py`. Скрипт возьмет файлы `images.csv` и `my_data.csv` и создаст файлы модели (`.pkl`).

## 🎮 Как пользоваться
1. Нарисуйте **Число 1**, **Знак** (+, -, *, /) и **Число 2**.
2. Нажмите зеленую кнопку **ВЫЧИСЛИТЬ**.
3. **Если модель ошиблась**:
   - Впишите правильный ответ в поле "Это: ..." под рисунком.
   - Нажмите **"Сохр."**.
   - Пример сохранится в `my_data.csv`. После накопления примеров перезапустите `train.py`.
4. Нажмите **ОЦЕНИТЬ ТОЧНОСТЬ**, чтобы увидеть статистику работы модели.

## 📂 Структура файлов
- `main.py` — Основной код приложения (интерфейс).
- `train.py` — Скрипт обучения модели.
- `images.csv` — Основной датасет с цифрами и знаками.
- `my_data.csv` — Файл, куда сохраняются ваши собственные примеры.
- `*.pkl` — Файлы обученной модели (создаются автоматически).
